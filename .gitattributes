# 统一换行符：仓库内一律 LF（照抄壳仓根 .gitattributes 的判定，脱离壳仓后自带一份）
#
# 为什么必须有它：本机 core.autocrlf=true ⇒ 没有本规则时，检出得到 CRLF 而脚本写出的是 LF，
# 于是任何「生成物与盘上比对」的门禁都得各自记得归一行尾。本文件把这个类从「靠记性」变成「靠规则」。
# 边界：text=auto 由 git 自行判定文本/二进制，二进制文件（png/svg 之外的位图）不受影响。
* text=auto eol=lf
